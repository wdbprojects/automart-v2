import SignInForm from "./sign-in-form";

const SignIn = () => {
  return (
    <main className="!w-full min-h-[calc(100svh-4rem)] flex flex-col items-center justify-center mx-auto">
      <SignInForm />
    </main>
  );
};

export default SignIn;
